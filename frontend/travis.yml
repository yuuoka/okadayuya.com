language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t CI=true yuuoka/okadayuya.com -f Dockerfile.dev .

script:
    - docker run yuuoka/okadayuya.com npm run test -- --coverage